from django.apps import AppConfig

class BandConfig(AppConfig):
    """
    Django AppConfig for the 'band' application.

    This configuration is used to register the app with Django and manage its settings.
    """
    name = 'band'

